tasks:
  - init: |
      cp .env.example .env
      npm install -g wasm-pack
      rustup default nightly
      rustup component add clippy rustfmt --toolchain nightly
      rustup target add wasm32-unknown-unknown wasm32-wasi --toolchain nightly
      sudo apt upgrade -y && sudo apt update -y && sudo apt autoremove -y
      sudo apt-get install -y libdbus-1-dev libgtk-3-dev libwebkit2gtk-4.0-dev libappindicator3-dev librsvg2-dev patchelf protobuf-compiler
      cargo install trunk wasm-bindgen-cli tauri-cli
    command: cargo build --release --workspace --target wasm32-unknown-unknown
